[tool.poetry]
name = "y"
version = "y"
description = "y"
authors = ["y"]
license = "y"
readme = "README.md"

[tool.poetry.dependencies]
python = "y"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
